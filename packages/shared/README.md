# shared

The contract between server and client: DTOs, `ServerEvent` (everything the
websocket can push), and the theme list. One file, no logic — if server and
web both need a type, it lives here and nowhere else.
